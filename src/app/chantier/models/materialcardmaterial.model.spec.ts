import { Materialcardmaterial } from './materialcardmaterial.model';

describe('Materialcardmaterial', () => {
  it('should create an instance', () => {
    expect(new Materialcardmaterial()).toBeTruthy();
  });
});
